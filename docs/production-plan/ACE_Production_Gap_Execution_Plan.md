# ACE Production Gap Assessment: Execution Plan (Final, v11)

Status: FOR APPROVAL. No coalition run starts until approved.
Scope basis: everything verified in this working session against the live codebase at `C:\GBS\Clients\State of NY\Finalrhtpdemo`.
v5 change: stakeholder documentation gap closed (Documentation Specialist + adversarial round). New: doctrine 9, contract C8, Phase-2 documentation specialist, deliverable D7, gate F.
v6 change: house coding guidance factored in. `AI-CODING-CONVENTIONS.md` verified in the repo and made binding (doctrine 10); evidence item 5 corrected (the repo has an engineering `docs/` tree; the gap is stakeholder coverage plus unwired enforcement); D7 integrates with the existing docs tree instead of inventing a parallel one; adversarial axis added for convention violations. (Conventions v2 + enforcement subsequently adopted and committed in-session.)
v11 change: final agentic-engineering review. Question asked: where will five parallel agents reading this same document still diverge? Four fixes: (1) the golden-path worked example — one domain traced end-to-end through all five pipeline stages into graph, SDE, and care plan — mandated as a spine artifact every specialist must trace their design through (the strongest anti-drift device available); (2) the 16-step Maria use case enumerated and traced to components as D1 sheet 4, so "complete against the vision" becomes verifiable instead of asserted; (3) DP-7 identity resolution semantics (survivorship, merge/unmerge, provider identity) — a ripple-into-everything gap the direction sweep missed; (4) agent-operations mechanics — concrete schemas instead of prose stubs, per-specialist context manifests and output templates, rubric-scored adversarial review with defined re-run triggers, C5 made mechanical.
v10 change: direction-completeness sweep. The pipeline gap's test — "given only this document, would a specialist make the choice we intend?" — applied to every workstream. Five more under-directed areas found and closed with binding direction packs (§6A): graph ontology + temporality (G1), signal/disposition semantics (G2), first agents + reuse direction (G4), care-plan acceptance oracle (G5), load-model parameters + non-functional budgets (X1/all). Doctrine says how to build; direction packs say what good looks like per domain; specialists design the rest.
v9 change: pipeline architecture round. The plan had delegated pipeline design detail to Phase-2 agents with only lane doctrine as direction — insufficient for load-bearing decisions. Added: a binding five-stage pipeline reference architecture (§4A) covering all inbound formats (FHIR, X12, flat file, C-CDA, HL7v2, JSON/webhook), an explicit staging + transformation tier, lane-agnostic transforms (same logic in batch AND stream), named social + behavioral ingestion paths with Part 2 segmentation applied at transformation time, and the record-to-graph propagation mechanism (projection via domain events, C10 + ADR-006). Also states explicitly which decisions are doctrine (binding now), which are specialist design (within doctrine), and which are ADRs (spine).
v8 change: WPC record completeness round (coalition + adversarial). The pipeline scope (G3) was bounded by machinery, not by a target record — added contract C9 (WPC Record Target Model: domain x granularity tier x source x consumer minimum-viable-record), a coverage-scoring deliverable (D1 sheet 3), and the rule that G3 completeness is measured against C9, never claimed from pipeline machinery existing. Evidence item 7 records the measured baseline (the seeded record is a 20-resource PA-shaped sliver; the WPC screens run on hardcoded TS today).
v7 change: the repo's own open backlog (`docs/remaining-work-and-test-plan.md`, offline items O-1…O-9) adopted into scope and mapped onto the gap areas; new gap area G7 (network adequacy completion + integration) and cross-cutting X5 (accessibility); ADR-005 (evidence persistence); the doc's gated track recorded as GB-1…GB-7 to avoid colliding with this plan's G-numbering; its test plan absorbed into acceptance criteria; dependency order aligned with the doc's own suggested order.

---

## 1. Objective

Produce an engineering-grade gap assessment plus build plan that takes the ACE platform from demonstrated demo (Tier B presentational surface) to production capability (Tier A engineering) for the 16-step Maria use case, at scale, deployable on any hyperscaler, without breaking the working demo at any point — and documented so every stakeholder group (executive, engineering, operations, deployment, compliance, enablement) can act on it.

### 1.1 The build, in one view (added in v8 — the totality, so scaffolding never obscures it)

**The center is the WPC record.** Everything else either feeds it, consumes it, runs it, or proves it:

- **FEEDS IT — G3 pipelines + O-1/O-2 + X2:** connectors (834, 837/835, CCD/QE, pharmacy, ADT, in-product screening) land data into the FHIR record per the C9 target model, with reconciliation gates and durable persistence. This is the build's foundation and it goes first.
- **CONSUMES IT — the intelligence layer:** G1 graph (relates the events of the record), G2 SDE (dispositions signals about the record), G5 care plan + O-7 content (plans from the record), G7 adequacy + O-6 (network reality joined to the record), G4 agents + O-8 (act on the record under governance). Each declares its minimum-viable record (C9.4); none can be more complete than what feeds it.
- **RUNS IT — X1 substrate + X3 portability:** three execution lanes, linear scale posture, any hyperscaler.
- **PROVES IT — the golden thread + tests + O-9:** the financial evidence chain (already the strongest Tier-A code), the absorbed test plan, accessibility, demo-stays-green.
- **Scaffolding in service of the above, deliberately bounded:** conventions v2 + ratchet (done, in-repo, no further plan time), documentation X4 (authored-now set only), approval gates. Scaffolding items get no new scope from here — every remaining hour of this plan's execution goes to the four build layers above.

### 1.2 Vision verification (new in v11 — completeness is traced, never asserted)

The platform vision is operationalized by two concrete artifacts already in hand: the **16-step Maria use case** (`ACE_UseCase_Dependencies.xlsx`, staged as Phase-0 input) and the **four-act demo arc** (Fragmented → Coordinated → Provable → Connected). The spine's first deliverable includes a traceability matrix — **D1 sheet 4: use-case step x components exercised x C9 domains/tiers required x phase where it runs on real engineering** — and the finalizer reports plan completeness ONLY as coverage of that matrix. A step no component claims, or a component no step exercises, is a finding, not a footnote.

## 2. Evidence base (verified, not assumed)

1. Code diligence executed: 223 of 223 unit tests pass across 33 suites, run independently in a clean environment. ~119K lines of TypeScript; 21 real API routes; real HAPI FHIR JPA server (Docker) seeded with the Maria bundle; Newman contract tests for CMS-0057-F; Playwright e2e.
2. Two-tier finding, confirmed against the owner's six hypotheses:
   - Tier A (production-track, tested): identity match engine (deterministic + probabilistic, thresholds 90/60, audited), consent opt-out seam, authz guard, goldenThread orchestrator + stages + medical necessity + DTR, policy corpus ingestion, network adequacy, FHIR validation utilities.
   - Tier B (presentational): knowledge graph (804-line hardcoded data file, no graph store, no Cypher execution), Signal Disposition Engine (918-line page, results hardcoded), agent coalition / orchestration (no runtime, demo scenario data only), dashboards (per-screen inline numbers), WPC population (seed scripts only, no pipelines).
   - Care plan generator: real 1,262-line service across five modules; zero tests; rigor at rules/template level.
3. Execution substrate absent by design: `start` script runs `next dev`; in-process Map stores; no queue / cache / worker deps; file-based state sync; `backend/` holds logs only. Engines are pure stateless functions (ideal for scale-out); the substrate to run them at volume does not exist yet.
4. House pattern established in code: the data-source seam (consent module: swap mock store for production source without changing callers). All builds must follow it.
5. Documentation state (corrected in v6, verified against the repo): the repo has a real engineering `docs/` tree (ARCHITECTURE.md, traceability.md, build-narrative.md, conformance-plan.md, current-state.md, remaining-work-and-test-plan.md, demo-plan.md, feature READMEs such as `src/lib/policy/README.md`). What is missing is everything beyond the engineering audience: no operator runbooks, no deployment guide, no API reference, no executive-readable current-vs-target statement, no compliance posture document, no enablement material. The RFI response at repo root is a sales artifact, not operations documentation.
6. WPC record baseline (new in v8, measured): the seeded FHIR record for Maria is 20 resources across 15 types (Condition x3, Organization x2, Practitioner x2, ClaimResponse x2, one each of Patient, Coverage, Observation, MedicationRequest, DiagnosticReport, ServiceRequest, Claim, ExplanationOfBenefit, Consent, Questionnaire, PractitionerRole). It is prior-auth-shaped, not whole-person-shaped: no Encounter, no AllergyIntolerance, no Immunization, no Procedure, no CarePlan, no CareTeam, no Goal, no Task, no RelatedPerson, no MedicationDispense, no SDOH Observations or QuestionnaireResponses, no DocumentReference, no behavioral-health content. The rich WPC screens are fed by hardcoded TypeScript (`mockData.data.patients.ts`, `patientRegistry.data1-3.ts`, `fhirCareTeamData.ts`, `patientContext.*`), not by the FHIR record. Pipeline-fed WPC completeness today is therefore effectively zero; the seed script is the only populator.
7. Coding guidance state (verified): `AI-CODING-CONVENTIONS.md` (repo root, 15 sections) is the project's binding coding standard: 400-line production file cap (500 for tests, seed/template files exempt), function ≤50 lines / complexity ≤10 / nesting ≤4, feature-first layout (`src/lib/<domain>` with `types.ts`, `index.ts`, `README.md`), data-is-not-code (seed data in `data/*.json`), single source of truth per type/schema/dataset, the BFF-only security invariant (browser calls only `/api/*`; no secrets in `NEXT_PUBLIC_*`; PHI-safe audit events), six mandatory AI guardrails (server-side only, deterministic-first, human-gated, PHI-safe, labelled decision-support, feature-flagged with graceful degradation), Tier-A offline vs Tier-B backbone-gated honesty ("a capability without a green test is asserted, not verified"), the traceability rule (every capability adds a row to `docs/traceability.md`), a 13-point definition of done, and a legacy debt register (86 files over cap, with prescribed splits). Enforcement is specified but **not wired**: `package.json` has no `check:sizes` / `check:all` / `pretest` scripts and CI has no size gate; referenced `AGENTS.md` / `CLAUDE.md` / `.eslintrc.enforcement.json` do not exist. Both G1 and G2 anchor files are themselves convention violations (`wholePersonGraphData.ts` 804 lines, SDE page 918 lines, inline data), and the debt register already prescribes the G5 split (`carePlanGenerator` → builder / validator / templates).

## 3. Scope

Active gap areas (6): G1 Knowledge graph + population + query layer. G2 Signal Disposition Engine (first real agent). G3 FHIR WPC ingestion pipelines. G4 Agentic orchestration runtime + agent library foundation. G5 Intelligent care plan hardening (clinical rigor + tests) + richer clinical criteria content. G7 Network adequacy completion + cross-domain integration (new in v7).
Parked (recorded, not worked): G6 Shared metric engine for dashboards.
Cross-cutting workstreams: X1 Execution substrate (three lanes). X2 Pipeline DAG layer (Airflow-compatible). X3 Hyperscaler portability + deployment configurator. X4 Stakeholder documentation (docs-as-code, audience-mapped; new in v5). X5 Accessibility + app-wide quality pass (new in v7).

### 3.1 Adopted backlog: `docs/remaining-work-and-test-plan.md` offline items (new in v7)

The repo's own open-work register is adopted, not duplicated — each O-item maps to exactly one owner in this plan:

| Backlog item | Maps to | Owner in Phase 2 | Notes |
|---|---|---|---|
| O-1 Durable evidence persistence | X1 + ADR-005 | Spine decides store; Pipeline pair builds | Replaces in-memory `defaultEvidenceStore()`; first concrete production store swap, proving contract C3 |
| O-2 Real data-source adapters | G3 / X2 | Pipeline + FHIR pair | Gold-card roster, denial-rate feeds, provider-directory loader — behind existing seams; the seam contract's first live exercise |
| O-3 NA dashboards (port four `providernet_analytics` views) | G7 | Graph & Context Architect (view-model discipline) + Pipeline pair | Dashboards consume shared view-models, not per-screen inline math — the G6 lesson applied even while G6 stays parked |
| O-4 NA geospatial heatmap (county choropleth, drill-down) | G7 | same | Copilot `focusCounties` already drives it |
| O-5 NA compliance-readiness report | G7 | same | One-click audit evidence; feeds the D7 compliance audience |
| O-6 Adequacy ↔ referrals ↔ prior-auth integration | G7 + C1 | Solution Integration Architect (spine) sets the shared provider model; specialists consume | Extends C1 with provider-context; links gaps → referrals → PA |
| O-7 Richer CQL criteria content | G5 | Care Plan pair (Medical Expert lens) | Engine exists; content work. Auto-approval stays gated on SME sign-off (GB-3) — content lands flagged not-SME-reviewed |
| O-8 Interactive stage-3 deepening | G4 | Agentic Systems Designer | The existing `/prior-auth` machine driven end-to-end from thread context — the journey lane's first real workload |
| O-9 Accessibility pass (whole app) | X5 | Finalizer spec + all specialists' acceptance criteria | axe-core in Playwright per surface, keyboard nav, aria/roles extended app-wide |

The doc's gated track (its G-1…G-7: live AI-DTR, LLM narration, SME sign-off, Tier-B backbone, conformance runs, live adequacy data, security review) is recorded in the matrix as **GB-1…GB-7** (renumbered here to avoid collision with this plan's gap areas) with blockers named — planned around, not built, until keys / infrastructure / SME time exist. Its test plan (§3 of the doc) is absorbed into D4 and per-row acceptance tests. Known drift: the doc says 145 vitest passing; this session measured 223/223 — the documentation specialist updates the doc as a D7 docs-stay-current action.

## 4. Architecture doctrines (binding on all specialists)

1. **Seam pattern.** Every engine lands behind an interface the existing screens already call; authored demo data remains a selectable mode (the Mock Data toggle becomes the seam switch). Demo stays green through every phase.
2. **Contracts before components.** No specialist designs until the spine publishes the contract register (Section 6).
3. **Three-lane execution.**
   - Batch lane: DAG pipelines. Steps are containers with declared inputs/outputs + idempotency keys. Airflow is the reference orchestrator (managed Airflow exists on AWS MWAA / GCP Composer / Azure ADF). Adapters to ADF / Step Functions only if a customer mandates them. Streams never run in Airflow.
   - Stream lane: event backbone. Partitioned queue, partition key = memberId (guarantees per-member ordering the SDE requires), idempotent consumers, DLQ + replay.
   - Journey lane: durable workflows for long-lived member journeys (multi-day human-in-the-loop waits, timers such as 72-hour scheduling windows), with retries + state.
4. **Scale doctrine: design for linear scale, prove at 10x pilot.** Stateless partitioned workers from day one; load-test gates at 1K/min sustained, 5K/min burst, 10K/min headroom; documented linear-scale argument (partitions + pods) to 100K/min. CPU-bound match scoring uses blocking keys (DOB / zip / phonetic) to bound candidates, worker_threads pool for scoring. HAPI posture: clustered nodes + tuned Postgres + async boundary validation; CQRS reads from projections.
5. **Portability doctrine: no proprietary control plane in the core.** Core services depend only on open protocols: Postgres wire, Kafka API, S3-compatible object storage, OIDC, container runtime. Anything hyperscaler-specific lives in the IaC layer (Terraform modules per cloud). Durable workflow engine must be self-hostable (Temporal-class), never Step Functions / Durable Functions in the core.
6. **FHIR store as a seam.** HAPI self-managed is the reference implementation; managed FHIR services (AWS HealthLake / Azure Health Data Services / Google Cloud Healthcare API) are conformance-gated adapters on the roadmap. A conformance test suite gates any swap.
7. **Deployment configurability, phased honestly.** v1: `deploy.config.yaml` (cloud, region, managed-vs-self choices per tier) driving per-cloud Terraform stacks through CI. v2: an admin-console Deployment screen that renders + drives the same engine. The screen is the face; IaC is the engine.
8. **Effort grammar.** S/M/L with explicit assumptions plus named spike tickets. No fake week-precision.
9. **Documentation doctrine (new): docs-as-code, audience-tagged, state-labeled.**
   - Docs live in the repo (`docs/`), versioned with the code, generated from source wherever possible (API reference from route definitions, contract docs from schemas, deployment guides rendered from `deploy.config.yaml`). Hand-maintained copies of generatable content are forbidden — they go stale.
   - Every document declares its audience and whether it describes **current state or target state**. No document may imply production capability that today exists only as demo surface. This is the documentation twin of the demo honesty guardrails.
   - Two classes, adversarially enforced: **authored-now** docs (truthfully writable from this session's verified evidence) and **build-gated** docs (a template plus acceptance criteria attached to the epic that makes them true — an epic is not done until its runbook section, API entries, and deployment notes exist). Nobody writes operational fiction for systems that do not exist yet.
   - Docs-stay-current is a regression contract like demo-stays-green: a PR that changes a seam, contract, or deployment surface without touching its doc fails review.
10. **House coding conventions are binding (new in v6; referent updated).** The governing standard is now `AI-CODING-CONVENTIONS.md` **v2** at repo root (adopted and committed in this session; v1 archived at `docs/archive/AI-CODING-CONVENTIONS.v1.md`, out of scope going forward). Enforcement is live: ratchet-aware size gate with `quality-baseline.json` (66 legacy files frozen, may only shrink), `check:types` / `check:sizes` / `check:all` / `pretest` wired into `package.json`, `convention-gates.yml` CI workflow, versioned pre-commit hook (`tools/hooks`), `AGENTS.md` + `CLAUDE.md` session entry maps. Every design and every build task complies with v2 as written, not as summarized here. Specifically inherited into this plan:
   - **Size and structure.** No new file over 400 lines (500 for tests); functions ≤50 lines, complexity ≤10. Specialist designs must state their file split up front; a design that lands as one big file fails review before it is coded.
   - **Feature-first layout + data-is-not-code.** Every new engine lands as `src/lib/<domain>/` with `types.ts`, `index.ts`, feature `README.md`; authored demo data moves to `data/*.json` loaded at runtime. This is convergent with the seam pattern: replacing `wholePersonGraphData.ts` and the SDE inline data with data files behind an interface simultaneously builds the seam and retires two convention violations.
   - **BFF-only invariant.** Graph queries, disposition calls, agent runtime interactions all go browser → `/api/*` → engine. No exceptions in any new surface.
   - **AI guardrails, all six.** The agent runtime (G4) and disposition engine (G2) are designed server-side only, deterministic-first (the LLM narrates on top of a reproducible engine path), human-gated per the autonomy dial, PHI-safe in model calls (references and codes, asserted in tests), labelled as decision-support, and feature-flagged with graceful degradation.
   - **Tier language reconciled.** The conventions' Tier-A (offline, always runnable) / Tier-B (backbone-gated) distinction is the repo's own honesty spine and maps cleanly onto this plan's production-track vs presentational finding. All plan documents adopt the repo's `BackboneNotConfiguredError` fail-loud pattern for anything backbone-dependent.
   - **Traceability + definition of done.** Every capability a specialist proposes carries its `docs/traceability.md` row and the 13-point DoD (tsc 0, vitest green, lint clean, `check-file-sizes.sh` exit 0) as acceptance criteria. Build-gated docs (doctrine 9) and traceability rows are the same discipline; the register wires them together.
   - **Legacy debt register respected.** Where the register already prescribes a remediation (carePlanGenerator split, mockData externalization), the plan adopts that prescription rather than inventing a competing one. No new code is added to any file already over cap.
   - **Enforcement gets wired.** The conventions specify gates the repo has not wired (`check:sizes`, `check:all`, `pretest`, CI size step). Wiring them is a small authored-now deliverable in this run, so the conventions stop being tribal knowledge.

## 4A. Pipeline reference architecture (new in v9 — binding on G3/X2; specialists design WITHIN this, they do not reinvent it)

**Five stages. Every feed, every domain, both lanes.**

1. **Land.** Raw capture of every inbound format, immutable, replayable: FHIR (API push/pull, bulk `$export` NDJSON), X12 (834 / 837 / 835 / 278), flat files (CSV / pipe-delimited SFTP drops — first-class, because CBO and social-sector data arrives this way), C-CDA/CCD XML from QEs, HL7v2 (ADT streams), JSON/webhook (screening tools, closed-loop referral platforms). Object storage, cataloged (source, received-at, batch id, checksum). Nothing is transformed here; landing is the audit + replay foundation.
2. **Stage + validate.** Explicit staging tier — structural validation per format (X12 envelope, CCD schematron, schema checks), a quarantine lane for rejects with a remediation workflow, PHI-safe rejection reporting. Bad records never silently vanish and never poison a load.
3. **Transform + enrich (in-flight processing).** Source adapters normalize to the canonical domain model (the `NormalizedPolicy` pattern, applied to every domain); terminology normalization (ICD-10, SNOMED, LOINC, RxNorm, Gravity Z-codes/value sets); identity resolution via the existing match engine (member link, provider NPI resolution) — the record attaches to the anchored identity, never to raw source ids; **consent + 42 CFR Part 2 segmentation labels applied here, at transformation time** — segmentation is a property of the data from the moment it is normalized, not a read-time afterthought.
4. **Conform + load.** US Core / profile validation with `$validate` gating; reconciliation gates (counts in = loaded + rejected, per batch, alarmed); upsert into the FHIR store with Provenance resources per load. The record is the single source of truth.
5. **Project + propagate — how domains reach the graph and everything else.** Every committed record change emits a domain event (C2 envelope, memberId-partitioned) via change-data-capture (ADR-006: transaction outbox vs FHIR Subscription; dual-write is banned). Downstream projectors consume independently: the **graph projector** (per-domain mapping spec: which resources become which node/edge types — the demo's 52-node/67-edge schema is the target seed; Part 2 data projects only as restricted nodes honoring its labels), the **SDE signal intake**, and **metric projections**. The graph is derived state — rebuildable from record replay, never a source of truth. This is the role-separation doctrine made mechanical.

**Lane-agnostic transforms.** Each domain declares its arrival modes — batch (monthly 834, daily/weekly claims, nightly rosters, CBO flat files, HMIS extracts), micro-batch (QE CCD queries), stream (ADT, screening submissions, referral-status webhooks, PGD). Stages 2–5 are pure functions per doctrine 7, packaged twice from one implementation: as DAG step containers (batch lane) and as stream consumers (stream lane). One transform codebase, both lanes — divergence between batch and stream logic is a defect class, banned by construction.

**Named social + behavioral ingestion paths (no longer implicit):**
- *Social:* in-product AHC HRSN screening (stream → SDOH T1 with provenance); closed-loop referral platforms (Unite Us-class — the NY SCN lead operates one; webhook/API + flat-file export); CBO flat files over SFTP (the lowest-common-denominator path, treated as first-class); housing/HMIS extracts; Z-code capture from claims (T3 shadow).
- *Behavioral:* BH claims (T3); BH clinical via QE feeds where consent allows (T2/partial T1); **SUD data exclusively through a Part 2-segmented lane** — labeled at stage 3, stored with segmentation, projected restricted, and never surfaced to signals or agents without a consent check at read time.
- *Member-reported:* app/SMS questionnaire responses (stream, T1, provenance-tagged as patient-generated).

**Who decides what (the answer to "are agents assumed to handle this"):**
- *Doctrine — binding now (this section, §4, C-register):* the five stages, staging tier existence, lane-agnostic transforms, segmentation-at-transformation, propagation-by-projection, formats supported, source-of-truth rules.
- *Specialist design — within doctrine (Phase 2):* per-source adapter specs, DAG definitions, per-domain graph mapping specs, quarantine/remediation workflow detail, per-feed reconciliation thresholds.
- *ADR — spine decides against the load model (Phase 1):* store choices (ADR-001, -002, -005), orchestration posture (ADR-003), CDC mechanism (ADR-006).

## 5. Decision register (all decided in Phase 1, against the load model)

- **ADR-001 Graph store.** Event-sourced TypeScript projections over HAPI vs dedicated graph DB (Neo4j-class). Decided with the load model (write rate vs traversal depth). Either way lands behind the graph seam.
- **ADR-002 Execution substrate.** Queue technology (Kafka-API vs Postgres-backed to start) + durable workflow engine (Temporal-class vs pg-boss state machines to start), with the migration seam named.
- **ADR-003 Pipeline orchestration.** Steps-as-containers + Airflow reference + adapter posture (vs standardizing hard on one orchestrator). Includes the batch/stream boundary rule.
- **ADR-004 Deployment target model.** IaC layout, first two clouds (proposed: AWS + Azure), managed-FHIR seam roadmap, configurator v1/v2 phasing.
- **ADR-005 Evidence + state persistence (new in v7, for O-1).** Append-only Postgres event table vs FHIR-persisted (AuditEvent / DocumentReference in HAPI) vs hybrid. Decided against the load model + auditability requirements (GB-7 security review is a consumer). Whatever wins becomes the template for retiring every in-memory Map store, in the seam-swap order the spine publishes.
- **ADR-006 Change propagation / CDC (new in v9, for §4A stage 5).** Transaction outbox vs FHIR Subscription (R4B/R5 topic-based) vs store-level CDC (Debezium-class on the HAPI Postgres). Dual-write from pipelines to record AND graph is banned regardless of choice — the record commits, the event follows, projectors consume. Decided with ADR-001/002 as one coherent set.

## 6. Contract register (published by the spine, binding)

C1 Person-context read API (what graph, agents, care plan consume). C2 Event envelope (signal in, disposition out; idempotency + ordering fields). C3 Seam convention (codified from the consent module). C4 FHIR profile set (US Core + CarePlan / ServiceRequest / Task; ReferralRequest R4 correction). C5 Demo-stays-green regression contract, mechanical (strengthened in v11): an automated Playwright walkthrough of the demo click path plus a dual-mode test — every seamed screen must render correctly with the mock toggle ON and OFF — run per phase; a red walkthrough blocks merge. C6 Event backbone contract (partitioning, delivery semantics, DLQ, replay). C7 Infrastructure abstraction contract (the five permitted protocol dependencies; everything else via IaC). **C9 WPC Record Target Model (new in v8 — decided by the spine BEFORE C1, because the person-context API is a projection of this record).** Four parts:
   1. **Domain inventory.** The whole-person domains the record must represent: demographics/identity, coverage/eligibility, encounters/utilization, conditions, medications (prescribed AND dispensed), labs/vitals, procedures, immunizations, allergies, care team/attribution, care plans/goals/tasks, referrals/service requests, SDOH (Gravity: screenings, Z-code conditions, goals, referrals), behavioral health including SUD with 42 CFR Part 2 segmentation, consent/directives, claims/financial (the golden thread chain), PA lifecycle, assessments/member-reported, caregiver/household (RelatedPerson), documents.
   2. **Granularity tiers, declared per domain per phase — the honesty scale.** T1 = discrete coded data (computable: graph, SDE, care plan can reason over it). T2 = document-level (CCD as DocumentReference: human-readable, attached, NOT computable). T3 = claims-inferred (derived shadows from 837/835: lagged, incomplete, utilization-biased). T0 = absent. A domain is never reported "covered" without its tier; T2/T3 never counts as T1.
   3. **Source-to-domain yield map.** What each feed actually delivers: 834 → coverage T1; 837/835 → utilization/condition/medication shadows T3; CCD/QE (Hixny-class) → mostly T2 with partial T1 (meds/problems/labs where coded); pharmacy feeds → dispense T1; ADT → encounter events T1 (stream lane); in-product screening → SDOH T1 with provenance. Part 2 segmentation and consent scoping are record-model-native from day one — retrofitting segmentation is a rebuild.
   4. **Consumer minimum-viable-record (MVR) declarations.** Every consumer (graph G1, SDE G2, care plan G5, adequacy G7, agents G4, golden thread) declares the domains and tiers it needs to function honestly. Pipeline completeness is measured as: % of each consumer's MVR fed from real pipelines at the declared tier (not from mock files). "Demo screens fed from the record, mock toggle off" is the G3 definition of done.
**C10 Projection + propagation contract (new in v9).** The domain-event catalog (event types per domain, C2 envelope, ordering + idempotency guarantees per projector), the per-domain graph mapping spec format (resource → node/edge type, label propagation incl. Part 2 restriction), projector rebuild semantics (any projector must rebuild from record replay), and the consumer registration rule (a new projector subscribes; it never reads another projector's store as source), and the evolution rule (events and profiles change additively only, with a version field and a stated deprecation window — a breaking change is a new event type).
C8 Documentation contract: the audience map, the doc register schema (audience x document x current/target label x owner x done-gate), the docs-done definition wired into every epic, the generation sources of record. The register's engineering rows absorb the existing `docs/` tree (ARCHITECTURE, traceability, conformance-plan, feature READMEs) as already-owned entries rather than duplicating them. C3 (seam convention) is codified jointly from the consent module and `AI-CODING-CONVENTIONS.md` §5/§7 so the two never diverge.

## 6A. Specialist direction packs (new in v10 — binding inputs; each pack is what good looks like, not the design itself)

**DP-1 Graph & Context (G1).** The ontology seed is fixed, not invented: node taxonomy from the demo schema (Person, CareTeam member, Condition, CareGap, SDOH barrier, Encounter/Event, Intervention/Referral, Coverage/PA, Agent action) with the 52-node/67-edge demo graph as the reference instance. Three semantic rules bind: (1) **temporality** — events carry time; the graph answers "what happened around what," so edges are dated and the journey is reconstructable; (2) **causality is asserted, never silently inferred** — a causal edge (barrier blocks care) carries provenance for who/what asserted it (screening answer, clinician note, agent hypothesis flagged as hypothesis); associative edges are the default; (3) **the five demo lens filters are the acceptance queries** — the projected store must answer them (whatever ADR-001 picks) with the D3 screen consuming real results. The graph informs reasoning; it never acts (role-separation doctrine restated as an acceptance test).

**DP-2 Signal Disposition (G2).** A signal is a typed event about a member from any projector: care gap opened/closed, ADT admit/discharge, screening result, missed appointment, referral stall, denial, PA status change. The disposition decision model is the demo's coordinated-touchpoint semantics made real: for each signal batch per member, decide **act now / suppress (with reason) / delay (to a coordination window) / bundle into a single touchpoint** — the "5 approved, 3 suppressed, 1 delayed, one coordinated touchpoint" screen is the acceptance shape. Disposition policy is **data, not code** (contact-frequency caps, channel preference, consent scope, priority scoring, workforce capacity), per the conventions' adapter pattern, so a state can tune it without a deploy. Every disposition is explainable (which policy fired) and audited. Ordering per member is guaranteed by the memberId partition (C6); fatigue rules are cross-signal, which is why the SDE consumes the stream lane, not point calls.

**DP-3 Agent runtime (G4).** First agents are named, not open: (1) outreach/engagement agent (executes SDE-approved touchpoints), (2) referral coordination agent (closes the referral loop, escalates stalls), (3) PA/documentation agent (drives stage-3/O-8 on the goldenThread machine). All three launch HITL: they propose, the existing work-queue inbox is the human gate — **reuse the goldenThread work-queue and evidence-viewer patterns; do not build a second inbox.** Unactioned HITL items carry escalation defaults (SLA per priority tier, then escalate up the care-team hierarchy, then park with an audit trail — never silently expire). Manifests per conventions v2 §10 (tool allowlist, autonomy tier, escalation, PHI posture). The autonomy dial is per-agent-per-deployment configuration; promotion beyond HITL is an operational decision recorded in the manifest, never a code change.

**DP-4 Care plan (G5).** The acceptance oracle, so "rigor" is testable: (1) golden fixtures — a reviewed set of member contexts with expected plan characteristics, signed off by the Medical Expert lens, run as regression; (2) property invariants — no goal without an intervention, no intervention contradicting an active contraindication, every clinical recommendation carries a guideline citation (ADA/USPSTF/HEDIS-class source id), SDOH barriers present in context must be addressed or explicitly deferred; (3) FHIR conformance — output maps to CarePlan/Goal/Task/ServiceRequest profiles per C4; (4) O-7 criteria content lands flagged not-SME-reviewed until GB-3 sign-off. The generator stays deterministic; any LLM narration sits on top per the guardrails.

**DP-5 Load model + non-functional budgets (X1, consumed by all).** Planning parameters for the spine (adjust with evidence, not silently): pilot = county scale ~50K members; state = NY Medicaid scale ~7M. Feed assumptions: 834 monthly full + weekly deltas; claims weekly; ADT continuous; screening continuous. Default p95 budgets, binding until the spine re-derives them: person-context read ≤ 300ms; graph lens query ≤ 2s; signal → disposition decision ≤ 5s; ADT receipt → signal ≤ 60s; 834 batch landed → loaded ≤ 4h; dashboard view-model read ≤ 1s. Retention: landing zone raw kept per audit policy (default 7 years, configurable); quarantine TTL 30 days with alarmed expiry. Tenancy: single-tenant per state deployment in v1 — isolation by deployment (X3 configurator), never by code branching.

**DP-7 Identity resolution semantics (new in v11 — consumed by pipelines, record, graph, everything).** The match engine is real; production identity needs three semantics the code does not yet carry: (1) **survivorship** — when two sources disagree on a field, a source-ranked rule set (config data, not code) decides the golden value, with per-field provenance retained; (2) **merge/unmerge** — when a possible-match (60–90 band) later resolves to a confirmed match, a merge event flows through C10 and every projector rekeys by replay (no in-place key rewrites); unmerge is the same event class in reverse, and both are audited — merge handling is why projector-rebuild-from-replay is non-negotiable; (3) **provider + organization identity** — NPI-anchored, same survivorship discipline, feeding G7's shared provider model (O-6). Directional default: the record stores source-attributed facts; the golden view is a projection — so survivorship rules can change without data loss.

**DP-6 Adequacy (G7).** Thresholds (time/distance, ratios, wait-time) are configurable data reviewed annually — never constants; dashboards consume shared view-models (the G6 lesson); the compliance report (O-5) is generated from the same view-models so screen and audit evidence can never disagree.

## 7. Coalition roster and phases

**Phase 0 — Repo gate (~10 min).** Probe `RHTP_Code_Base`, `reference-implementation-cms0057f`, PA folders: engine existence, repo topology (one repo vs integrate three), any existing queue/worker/IaC, any existing documentation trees, and whether `AI-CODING-CONVENTIONS.md` (or a variant) governs those repos too. Matrix rows adjust before any design. The conventions file and `docs/ARCHITECTURE.md` + `docs/traceability.md` are staged as mandatory inputs to every subsequent agent (the conventions' own §0 rule, applied to this coalition).

**Phase 1 — Spine (one combined decisive run): Overall Solution Architect + Solution Integration Architect + Performance & Scalability Engineer + Platform & Portability Engineer.** Outputs: load model; ADR-001..006 decided; contracts C1..C10 stubbed — **C9 first** (the WPC Record Target Model bounds G3 and defines what C1 projects; every consumer's MVR declaration is collected here); C8 stub = audience map + register schema; C1 stub includes the O-6 shared provider-context extension and the consent-enforcement statement (every read scoped by requestor purpose + consent state, enforced by the existing authz guard pattern). **Spine outputs are concrete artifacts, never prose stubs (new in v11): JSON Schema for the C2 envelope with one sample event instance per domain; the C9 tier declarations as a table; the D1 sheet-4 trace matrix; per-specialist context manifests (exact files, contracts, and direction pack each agent loads — and what it must NOT load); per-specialist output templates (fixed section schema, output capped at a size the finalizer can merge); and the golden-path worked example — one domain traced end-to-end (an A1c result lands via QE feed → staged → normalized → loaded as Observation with provenance → domain event emitted → graph edge projected → signal raised → disposition decided → care plan updated → evidence recorded). Every Phase-2 specialist must trace its design through the golden path and state where it participates; a design that cannot is incomplete by definition.** Inputs: session evidence, Phase 0 results, staged code, `docs/remaining-work-and-test-plan.md`.

**Phase 2 — Specialists (5 parallel runs, inside the contracts and their §6A direction packs).** Each specialist also owns the O-items mapped to it in §3.1 (O-1/O-2 to the Pipeline pair, O-3/O-4/O-5 to Graph+Pipeline for G7, O-7 to the Care Plan pair, O-8 to the Agentic Designer); O-9 acceptance criteria attach to every surface any specialist touches.
- Graph & Context Architect (G1): graph projector per §4A stage 5 + C10 (consumes domain events; per-domain mapping specs with the demo's 52-node/67-edge schema as target seed; Part 2 restricted-node handling), query layer per ADR-001, rebuild-from-replay proof, seam for the D3 screen.
- Pipeline + FHIR Engineering & Validation pair (G3 + X2): connector/ETL architecture (837 / 834 / CCD / QE / pharmacy / ADT / screening feeds), DAG definitions on the container-step model, reconciliation gates, US Core profiling, validation harness with $validate gating, what seed tooling graduates — all designed inside the §4A five-stage reference architecture (adapter specs, DAG definitions, quarantine workflows, reconciliation thresholds; never a competing architecture). **Scope is bounded by C9: the pair delivers the phased domain-x-tier coverage plan (which domains reach which tier, from which source, in which phase) and is measured against consumer MVRs — never against pipeline machinery existing. Definition of done: WPC screens render from the record with the mock toggle off.**
- Agentic Systems Designer (G2 + G4), domain panel folded in as mandatory stress criteria (journey lens, longitudinal communication lens, clinical workflow lens: "could my agent live in this runtime unchanged?"). Disposition service design (stream lane) + minimal agent runtime (journey lane), HITL-first per the autonomy dial; goldenThread orchestrator named as the reuse foundation.
- Care Plan pair (G5): Medical Expert lens sets clinical rigor criteria (evidence base, contraindications, safety gates); engineering lens produces hardening plan + test-suite spec + FHIR conformance validation.
- Documentation Specialist (X4, new): builds the doc register against the audience map; authors the authored-now set (Section 8, D7) from session evidence + spine outputs; produces templates + docs-done acceptance criteria for every build-gated doc and hands them to the other four specialists to embed in their matrix rows and epics.
Every specialist output includes: matrix row, build approach + reuse map, file-level touchpoints, acceptance-test names, scale posture (throughput, partition key, idempotency, backpressure), portability posture (protocol dependencies only), S/M/L + assumptions, spike tickets, the docs-done entries for its epics, **and a convention-compliance statement (new in v6): proposed file layout under the 400-line cap, data externalized to `data/`, BFF route surface, AI-guardrail posture where AI is involved, traceability rows, DoD gates as acceptance criteria**.

**Phase 3 — Adversarial review (one run + up to two targeted re-runs), rubric-scored (new in v11).** Each specialist output is scored against a fixed rubric: direction-pack compliance (every DP item pass/fail), contract compliance, convention compliance, golden-path participation stated, budgets honored, matrix row complete. Re-run triggers are mechanical, not judgment calls: any direction-pack violation, or two or more rubric failures, sends that output back with findings attached; a third failure escalates to the owner rather than burning the revision budget. Attack axes: under-engineering (service coat on demo theater), over-engineering (platforms nobody needs by June), sequence realism vs the Sept–June window, seam compliance, demo-green, performance (single-node HAPI, in-request execution, in-memory state, unblocked matching, missing DLQ, ordering violations), portability leaks (proprietary control-plane dependencies in core), **pipeline-architecture axes (new in v9): batch/stream logic divergence, dual-writes to record + projection, segmentation applied at read instead of transform, flat-file paths treated as second-class, projectors that cannot rebuild from replay, domains landing without C9 tier declarations**, **direction-pack compliance (new in v10): designs that ignore or contradict their §6A pack, budgets dropped without spine re-derivation, disposition policy hardcoded, a second inbox built, causal edges without provenance**, **documentation axes: audience-less documents, doc bloat (authoring runbooks for unbuilt systems), target-state prose passable as current-state, generatable content hand-written, epics missing their docs-done gate**, **convention axes (new in v6): designs that would breach the 400-line cap or ship inline data, BFF bypasses, AI-guardrail violations (client-side model calls, un-gated recommendations, PHI in prompts, missing deterministic fallback), capabilities without traceability rows, additions to legacy over-cap files, Tier-A behavior claimed as Tier-B conformance**.

**Phase 4 — Finalize (me).** Merge; reconcile; apply dependency order, now aligned with the backlog doc's own sequencing: **O-1/O-2 + pipelines first** (durable persistence + real-shaped data make everything downstream production-real) → graph → SDE → G7 adequacy experience (O-3/O-4/O-5) → O-6/O-8 integration + stage-3 → care plan + O-7 content → agent runtime → **O-9 + test build-out proves it** (X-workstreams run beneath throughout); assemble the documentation pack from all specialist outputs; build deliverables; QA (including a current-vs-target label audit on every D7 document); deliver + commit to the client folder.

## 8. Deliverables

D1 Gap Matrix workbook (xlsx): rows for G1–G5 + G7 + parked G6 + X1–X5 + the O-items as sub-rows under their owners + GB-1…GB-7 as blocked rows with named blockers; columns: current state, code evidence (file:line), target architecture, build approach + reuse map, contracts touched, scale posture, portability posture, dependencies, effort (S/M/L + assumptions), risks, acceptance tests, docs-done entries; sheet 2: spike/epic backlog; **sheet 3 (new in v8): WPC coverage matrix — domain x tier (T0–T1) x source x phase x consumer-MVR requirement, scored current-state vs each phase's target, the single answer to "how complete is the record"**. D2 ADR-001..006 (markdown). D3 Contract stubs C1..C10 (markdown). D4 Load-test spec (k6 scenarios + gates) + the absorbed test plan from `remaining-work-and-test-plan.md` §3 (route-level 401/403/400/422/200 + PHI-safe assertions per BFF route, axe-core Playwright checks, regression guards: policy corpus-accuracy + adequacy seed-spread). D5 Deployment config spec (deploy.config.yaml schema + IaC module layout, v1/v2 phasing). D6 One-page executive brief.

**D7 Stakeholder Documentation Pack (new), organized by audience, every document state-labeled:**

| Audience | Authored now (from verified evidence) | Build-gated (template + docs-done criteria on the epic) |
|---|---|---|
| Executive & state program | Capability brief (extends D6); **current-vs-target honesty ledger** (what is demo surface vs production engineering today — the plain-language twin of the two-tier finding); phased roadmap one-pager | Quarterly progress narrative template |
| Engineering | Architecture overview extending `docs/ARCHITECTURE.md` (as-is + target, seams marked — updates the existing file, does not fork it); developer onboarding guide (repo map, seam how-to, test strategy, demo-stays-green rules, pointer to `AI-CODING-CONVENTIONS.md` as the binding standard); convention gate wiring spec (`check:sizes` / `check:all` / `pretest` scripts + CI size step per conventions §15, currently unwired); traceability rows for every planned capability appended to `docs/traceability.md`; ADRs + contracts (D2/D3 cross-referenced) | Generated API reference (from route definitions, wired into CI); per-engine design notes; feature `README.md` per new `src/lib/<domain>` module |
| Operations / SRE | Runbook framework + skeleton per lane (batch DAG ops, stream backbone ops, journey workflows); observability guide skeleton (SLOs derived from the load model) | Full runbooks (incident response, DLQ replay, backpressure response, HAPI cluster ops) — written as each lane lands |
| Deployment | Deployment guide structure rendered from `deploy.config.yaml` (AWS + Azure first); environment matrix; upgrade/rollback procedure template | Per-cloud validated walkthroughs — written when each Terraform stack first deploys clean |
| Compliance & audit | Security + privacy posture statement (consent model, 42 CFR Part 2 handling, provenance, audit trails — grounded in the tested consent/authz code); data governance summary | Audit evidence catalog — populated as controls go live |
| Enablement & field | Demo operator guide (safe click paths, mock/seam toggle, honest-answer scripts for "is this built?"); capability FAQ mapping demo scenes to engineering state | Training walkthroughs per module — written as each engine replaces its mock |

Plus the doc register itself (the C8 artifact): one table, audience x document x state label x owner x done-gate, committed at `docs/REGISTER.md`.

## 9. Activity estimate (this plan's production, not the build)

Lean configuration (recommended): ~80–100 minutes wall clock, roughly 1.0–1.3M tokens. Composition: Phase 0 ~10 min; spine ~10 min (now five ADRs + provider-context); specialists parallel ~12–15 min (five runs, each carrying its O-items); adversarial + revision budget ~10–14 min; finalize/assemble/QA/deliver ~28–38 min. Delta vs v6: +10 min, +100–150K tokens for the adopted backlog (no new runs — O-items ride inside existing specialists; G7 splits across two of them). v6 (conventions) added no separate run. Build-effort estimates inside the matrix follow the S/M/L + spikes grammar. Full configuration: ~115 min, 1.4–1.7M tokens.

## 10. Approval gates

A. This document as the governing plan. B. Lean configuration at the stated estimate. C. ADR decisions delegated to the Phase-1 spine (vs escalated to you per-ADR). D. First two IaC target clouds: AWS + Azure (Google as fast-follow). E. Deliverables committed to `Finalrhtpdemo` (proposed subfolder: `docs/production-plan/`, with D7 seeding the repo's permanent `docs/` tree). F. Documentation scope as defined in D7: authored-now set written in this run; build-gated set delivered as templates + epic acceptance criteria, not authored as fiction now. G. `AI-CODING-CONVENTIONS.md` v2 binding on all coalition output (doctrine 10). Gate wiring is already done in-repo (ratchet, scripts, CI, hook, AGENTS.md); the remaining rule stands: G1/G2/G5 builds retire their anchor files' frozen violations by extraction rather than extending them, and the baseline only shrinks. H (new in v7). Backlog adoption as mapped in §3.1: O-1…O-9 owned inside existing specialist runs (G7 + X5 as the only new scope labels), GB-1…GB-7 planned around but not built, dependency order per the doc's own sequencing (O-1/O-2 first), and the 145-vs-223 doc drift fixed as a D7 action. I (new in v8). The WPC Record Target Model (C9) as the binding boundary of the pipeline build: domain inventory + granularity tiers + source yield map + consumer MVRs decided by the spine before C1; G3 completeness reported only via the D1 sheet-3 coverage matrix; T2/T3 never presented as T1; scaffolding workstreams take no new scope from v8 forward. J (new in v9). The §4A pipeline reference architecture as binding direction: five stages incl. explicit staging tier, lane-agnostic transforms, segmentation at transformation time, propagation by projection only (C10, ADR-006, dual-write banned), social/behavioral/flat-file paths first-class — specialists design within it, never around it. K (new in v10). The §6A direction packs DP-1…DP-7 as binding specialist inputs, including the named first agents (HITL, reusing the goldenThread work queue), the care-plan acceptance oracle, identity survivorship/merge semantics, and the default non-functional budgets (spine may re-derive with evidence; specialists may not drop them). L (new in v11). The agentic-execution mechanics: spine publishes concrete artifacts (schemas, context manifests, output templates, the golden-path worked example every specialist traces through), the 16-step trace matrix as D1 sheet 4 with completeness reported only against it, rubric-scored adversarial review with mechanical re-run triggers, and C5 as an automated dual-mode walkthrough.

On approval: Phase 0 starts immediately; you receive the assembled package at Phase 4 with the adversarial findings appended.
