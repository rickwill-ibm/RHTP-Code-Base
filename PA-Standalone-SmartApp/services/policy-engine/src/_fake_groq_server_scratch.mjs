// Leftover scratch file from verifying the Groq-compatible request/response
// shape (openai SDK + custom baseURL + json_object mode) — safe to delete.
// (Left here empty because the tool that created it couldn't remove it.)
